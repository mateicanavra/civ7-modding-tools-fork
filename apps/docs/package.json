{
  "name": "@civ7/docs",
  "private": true,
  "scripts": {
    "dev": "bunx docsify-cli serve ./site -p 4000",
    "docs": "bunx docsify-cli serve ./site -p 4000",
    "build": "echo 'docs: no build step required (Docsify serves ./site)'",
    "fix:links": "bun run scripts/fix-links.ts",
    "clean": "rimraf dist",
    "test": "vitest run --project docs"
  },
  "devDependencies": {
    "docsify-cli": "^4.4.4",
    "rimraf": "^6.0.1",
    "vitest": "^3.2.4"
  }
}


