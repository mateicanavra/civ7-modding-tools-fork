---
title: Migration Notes
---

We migrated from Docsify to Mintlify. Legacy content remains under `/apps/docs/site` until fully ported.

Key changes:

- New structure uses `docs.json` and `pages/` with `.mdx` files.
- Static resources now live in `public/`.
- CLI `docs serve` runs `mintlify dev`.


