{
  "name": "@civ7/example-mod",
  "private": true,
  "version": "0.0.0",
  "description": "Example Civ7 mod workspace colocated with the tools; builds to ./mod and uses the Civ7 SDK.",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "node ./scripts/build.mjs --watch",
    "check": "node -e \"console.log('No checks defined for this mod workspace yet')\"",
    "clean": "rimraf mod",
    "test": "node -e \"console.log('No tests for this mod workspace yet')\""
  },
  "dependencies": {
    "@mateicanavra/civ7-sdk": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
