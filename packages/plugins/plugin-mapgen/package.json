{
  "name": "@civ7/plugin-mapgen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts && pnpm run build:mod",
    "build:mod": "bun run scripts/build.ts",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.9.2",
    "@types/node": "^24.2.1",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
