{
  "name": "@civ7/plugin-mapgen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && pnpm run build:mod",
    "build:mod": "bun run scripts/build.ts",
    "dev": "tsc -w -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@civ7/plugin-mods": "workspace:*",
    "@civ7/plugin-files": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^24.2.1",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
