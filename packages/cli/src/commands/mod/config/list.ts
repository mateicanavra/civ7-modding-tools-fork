import ListConfigBase from '../../../subtree/ListConfigBase.js';

export default class ModConfigList extends ListConfigBase {
  static summary = 'List stored mod subtree configurations';
  protected domain = 'mod';
}
