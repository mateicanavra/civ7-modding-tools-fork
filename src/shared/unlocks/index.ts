/**
 * Shared unlocks that are truly generic and not tied to specific entities.
 */

import { UnlockBuilder } from "@mateicanavra/civ7-sdk";

// Collection of shared unlocks
export const sharedUnlocks: UnlockBuilder[] = []; 